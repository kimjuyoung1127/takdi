"use client";

import type { PreviewShellProps } from "./preview-shell";
import { PreviewShell } from "./preview-shell";

export function RemotionPreview(props: PreviewShellProps) {
  return <PreviewShell {...props} />;
}
