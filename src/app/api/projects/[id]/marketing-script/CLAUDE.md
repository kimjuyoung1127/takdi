# marketing-script/
Shortform preview marketing-script API route.

## Files
- `route.ts`: async POST/GET route for generating and polling marketing-script jobs

## Convention
- Only `shortform-video` projects may use this route.
- Persist outputs as `ExportArtifact.type = "marketing-script"`.
