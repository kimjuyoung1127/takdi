import type { RemotionInputProps } from "@/types";
import { ShortformComposition } from "./shared";

export function TakdiVideo169(props: RemotionInputProps) {
  return <ShortformComposition {...props} orientation="16:9" />;
}
