import type { RemotionInputProps } from "@/types";
import { ShortformComposition } from "./shared";

export function TakdiVideo1x1(props: RemotionInputProps) {
  return <ShortformComposition {...props} orientation="1:1" />;
}
