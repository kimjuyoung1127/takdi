import type { RemotionInputProps } from "@/types";
import { ShortformComposition } from "./shared";

export function TakdiVideo916(props: RemotionInputProps) {
  return <ShortformComposition {...props} orientation="9:16" />;
}
