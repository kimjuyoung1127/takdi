# Self Review

- [x] 홈, 공용 레이아웃, 프로젝트/설정, 컴포즈 공통 UI의 사용자 노출 문자열을 한국어로 통일했다.
- [x] mode/status/action 라벨은 메시지 사전 바깥의 하드코딩을 줄이고 공통 메시지 키를 사용한다.
- [x] `I18nProvider`에는 직렬화 가능한 메시지 객체만 전달하고, 동적 문자열은 포맷 헬퍼로 처리한다.
- [x] 현재는 `ko`만 활성화했지만 `src/i18n/messages/en.ts`를 추가하면 확장 가능한 구조다.

## 남은 리스크
- 브라우저 수동 스모크는 이번 턴에서 수행하지 못했다.
- 새로 추가되는 사용자 노출 문구는 반드시 메시지 사전에 넣는 규칙을 유지해야 한다.
