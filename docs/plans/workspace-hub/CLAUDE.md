# workspace-hub plan docs

This folder tracks the phased rollout for workspace home, projects, and settings.

## Process
- Update phase doc status before coding
- Implement one phase group at a time
- Write self-review notes after each phase
- Record validation results after `typecheck` and `build`
